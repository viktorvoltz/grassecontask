import 'package:flutter/material.dart';

class DashBoard extends StatelessWidget {
  const DashBoard({this.icon, this.name, Key? key }) : super(key: key);
  final Icon? icon;
  final String? name;

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}